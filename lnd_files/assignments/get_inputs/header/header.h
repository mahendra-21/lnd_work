#include <stdio.h>
#include <stdio_ext.h>
#include <stdlib.h>

#define SIZE 128
#define LIMIT 3

int get_number(void);
int validate_for_int(char *str);
int my_atoi(const char *str);
char *get_string(void);
